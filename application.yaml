apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: my-app
spec:
  source:
    chart: my-chart
    targetRevision: "1.0.0"
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  project: default
